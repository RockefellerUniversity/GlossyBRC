PCT_R1_TRANSCRIPT_STRAND_READS:
  definition: The fraction of reads that support the model where R1 is on the strand
    of transcription and R2 is on the opposite strand. For unpaired reads, it is the
    fraction of reads that are on the transcription strand (out of all the reads).
  course_links: NA
